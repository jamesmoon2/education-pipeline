# Phase 2 — Per-Module Drafting and Section-Scoped Repair

**Goal:** Lift the one-response ceiling on course length. Draft becomes a skeleton call plus one model call per outline module, run by the daemon at bounded parallelism and assembled deterministically into the unchanged draft response; repair gains a section scope. One draft approval, no new stage, legacy Markdown untouched. Design: [`../specs/2026-09-18-per-module-drafting-design.md`](../specs/2026-09-18-per-module-drafting-design.md).

**Source:** the 2026-09-17 opportunity map (reviewed at `d85be72`), "Build plan · Phase 2". Threads are numbered as there. Line anchors are as of `55cfc82` (post Phase 1).

**Method:** strict TDD; one subagent writes the failing tests and a different one makes them pass; the manager reviews diffs, never transcripts. Every thread ends on the full pytest suite green, plus `npm run build` and `npm run test` when `web/` changed; Playwright only where a thread says so. A thread that grows past ~800 changed lines or 12 files is split.

**Baseline at open:** pytest 1775 passed / 1 skipped (46 s, Python 3.11). `runs.py` 2179 lines, `prompts.py` 1582, `daemon/jobs.py` 901, `guides/canonical.py` (splice_module at :50). vitest 554, `npm run build` clean, Playwright 87 (measured before T27/T25 changed `web/`).

**Audit ledger:** [`../specs/2026-09-18-phase-2-per-module-drafting-post-milestone-audit.md`](../specs/2026-09-18-phase-2-per-module-drafting-post-milestone-audit.md)

## Threads

| ID | Thread | Exit criteria | Status |
| --- | --- | --- | --- |
| T20 | Design spec | Spec in the repo's shape with the owner decisions settled by default; adversarial review against `runs.py` invariants applied; every cited line reference verified; this plan lists T21–T28. | - [x] |
| T21 | Module draft prompt and assembly | `check_skeleton` in `parse.py`; `compile_guide_v1_skeleton_prompt`, `compile_guide_v1_module_draft_prompt` with snapshot tests (module-prefixed ids, contribution lists); `assemble_guide` in `canonical.py` is byte-deterministic and refuses renames, missing/extra modules, contribution conflicts and cross-module id collisions naming both owners; `splice_module` shares its internals. | - [x] |
| T22 | RunStore module lifecycle | `draft_unit_paths`, skeleton + module prompts, unit ingest/edit, `assemble_draft`, `draft_progress`, unit manifest events, per-module staleness against the contract file, `next_action` unit arms inside the draft slot plus the 7b outline-changed rebuild arm (checked against the Phase 1 table; any pin change flagged); characterization table for unit states; resume from the workspace alone. | - [x] |
| T23 | Job fan-out and bounded parallelism | `Job.unit/module_id/batch_id`; `active_unit_for` beside an unchanged `active_for`; worker pool honouring `parallelism` (default 2, cap 4) with the dispatcher-side batch admission rule (wait, never re-queue); batch cancel; per-module salvage; fake-provider tests show bounded concurrency and one failure not blocking the rest. | - [x] |
| T24 | API and CLI surface | `POST /v1/jobs` batches draft modules (with `modules` subset; job-shaped response plus `batch_id`/`jobs`); unit ingest/edit/assemble routes; `draft_progress` in run status; `run --wait`/`--modules`, `status` prints `draft: k of N modules`; server and CLI tests. | - [x] |
| T25 | Cockpit: module progress | Board draft stage shows skeleton and per-module state with run/rerun/paste; batch status and cancel; stage viewer with module anchors and per-module change diff; Settings `parallelism`; `continueRun` handles batches; vitest green; `full-run.spec.ts` covers the unit paste path. | - [x] |
| T26 | Section-scoped repair, engine | `RepairScope`, `write_scoped_repair_prompt`, section repair prompt, `splice_section` byte-preserving siblings, findings→section mapping in `repair_modules_payload`, `repair_section` in manifest/advance/CLI. | - [x] |
| T27 | Section-scoped repair, cockpit | `ModuleRepairControl` is a module+section scope picker with findings preselecting scope; vitest; `blueprints.spec.ts` covers a section repair. | - [x] |
| T28 | Fixture, docs, closeout | `build_example.py` drives the unit path; `draft.guide.json` and `repair.guide.json` canonicalized together and the export regenerated if bytes change; interactive-guides and install docs updated; post-milestone audit written with accepted limitations. | - [x] |

## Order and parallelism between threads

T21 and T26 touch disjoint behaviour (module assembly vs section splice) and share only `canonical.py` and `prompts.py`; they run in parallel worktrees and merge in that order. T22 → T23 → T24 are sequential (each consumes the previous API). T25 and T27 are both cockpit and run in parallel after T24. T28 closes.

## Anchors at open

- Whole-guide draft prompt: `runs.py:1480` (`write_draft_prompt`), `runs.py:1503` (`_guide_v1_draft_artifact`), `prompts.py:856` (`compile_guide_v1_draft_prompt`), `prompts.py:530` (output contract lines).
- Module repair: `runs.py:1776` (`write_module_repair_prompt`), `runs.py:1110` (`repair_scope`), `runs.py:1124` (`_spliced_scoped_repair`), `prompts.py:1188` (module repair prompt), `prompts.py:1260` (path-prefix filter), `guides/canonical.py:50` (`splice_module`).
- Ingest/approve: `runs.py:1162` (`ingest_response`), `runs.py:1204` (`edit_response`), `runs.py:1051` (`approve_stage`), `runs.py:1837` (`_write_prompt`), `runs.py:2050` (`_append_event_locked`).
- Contract: `guides/contract.py:321` (`extract_outline_contract`), `:331` (`build_guide_contract`, sorted keys).
- Daemon: `daemon/jobs.py:45` (`Job`), `:367` (`JobRunner.execute`), `:423` (ingest call), `:703` (`Worker`), `:729-745` (enqueue guard), `:862` (`effective_timeout_seconds`); `daemon/server.py:88-152` (`enqueue_stage`), `:600-611` (job routes); `daemon/read_api.py:389` (`run_status_payload`), `:545-600` (`repair_modules_payload`).
- Cockpit: `web/src/components/PrimaryAction.tsx:84-220`, `web/src/lib/continueRun.ts:96-184`, `web/src/components/ModuleRepairControl.tsx`, `web/src/pages/RunBoardPage.tsx:161-170`.

## Closeout log

(One line per thread as it lands: what changed, test counts, accepted limitations.)

- **T20** landed: design spec (538 lines), this plan, and the ledger skeleton. Two code maps (engine; daemon/CLI/cockpit) preceded the draft; an Opus adversarial pass then found three blocking flaws and seven must/should-changes, all applied: a skeleton cannot pass `parse_guide` (cardinality, coverage and interaction rules), so it gets a dedicated `check_skeleton`; the unit `next_action` arms live inside the draft slot of the unbound-stages loop and yield to an existing response file, which keeps the whole-guide drop and the characterization pins intact; per-module staleness hashes against the immutable contract file, with a separate, explicitly scoped rebuild arm (7b) for outline re-approval before draft approval; `active_for` keeps matching module jobs; batch admission is dispatcher-side and waits rather than re-queues; the batch enqueue response stays job-shaped; the example fixture's two committed guide responses are canonicalized together. A Sonnet pass verified all 59 cited line references. Suite untouched at 1775 passed / 1 skipped.
- **T21** landed: `check_skeleton` (`guides/parse.py:249`, sharing `_decode_root`/`_check_root` with `parse_guide`; `skeleton.*` diagnostics; skips the coverage and interaction arms a sectionless guide can never satisfy), `assemble_guide` + `AssemblyError` (`guides/canonical.py:252`, `:19`; `splice_module` now calls the same `_load_module_fragment`/`_check_fragment_id`/`_parse_merged_guide` helpers, behaviour unchanged; cross-module id collisions are found before the strict parse and name both owners; contributions merge skeleton-first then module order), `compile_guide_v1_skeleton_prompt` (`prompts.py:1021`) and `compile_guide_v1_module_draft_prompt` (`prompts.py:1090`) built from shared draft/repair section helpers so the existing draft prompt's pinned bytes are unchanged. Red: 31 tests (Sonnet); green: Opus, one mutation of three not caught (reversed contribution order) and pinned. Suite 1807 passed / 1 skipped. Diff 1415 lines over six files, 645 of them tests; not split because the three pieces are one contract. Accepted: `check_skeleton` skips `link.unknown_internal_target` (targets live in sections that do not exist yet; the merged guide's strict parse still catches a dangling link); the module draft prompt does not itself run `check_skeleton` — that gate is `write_module_draft_prompts` in T22.
- **T26** landed (in parallel with T21, merged after it): `RepairScope` (`run_core.py:34`, re-exported from `runs` and the package root), `splice_section` (`guides/canonical.py`) on the same scoped-fragment helpers as `splice_module` and `assemble_guide` after the merge resolution unified the two threads' helper sets with every refusal string byte-identical, `compile_guide_v1_section_repair_prompt` sharing `_scoped_repair_inputs` with the module prompt, `write_scoped_repair_prompt` as the one writer with `write_module_repair_prompt` a thin alias, `repair_scope()` → `RepairScope | None`, `_spliced_scoped_repair` branching on `section_id`, `repair_section` in manifest events only for section scopes, `repair_modules_payload` with per-module `sections` and `module_level_findings` (attribution in a pure `finding_scope`), `advance_run(repair_section=)`, the route key, and CLI `--repair-section` (usage exit 2 without `--repair-module`). Red: 34 tests (Sonnet); green: Opus, three mutations caught, one gap (`module_level_findings` only type-asserted) pinned. Suite on the merged branch 1838 passed / 1 skipped. Accepted: `stage_content`'s `repair_scope` stays module-only (existing pin); `web/src/api/types.ts` still types `repair_scope` as `{module_id}` and omits the new keys, deferred to T27 (extra JSON keys are not a `tsc` error); no user-facing doc described `--repair-module`, so none gained the section flag (T28 adds one line to `docs/interactive-guides.md`).
- **T27** landed (in parallel with T22): `ModuleRepairControl` is a module+section scope picker ("Whole module" first; a section is preselected only when it is the sole section with open findings and there are no module-level findings, and a note explains when module-level findings force whole-module scope); `postAdvance` sends `repair_section`; the stage viewer's pending-scope copy is section-aware; `stage_content`'s `repair_scope` now carries `section_id` (the T26 accepted limitation, closed here with a widened server pin plus a section case). Red: 8 vitest tests (Sonnet); green: Sonnet. Gates on the merged branch: pytest 1839 passed / 1 skipped, `npm run build` clean, vitest 563 passed; Playwright `blueprints.spec.ts` 5/5 in the worktree including the new "one weak section regenerates in place" case (run with the `PLAYWRIGHT_CHROMIUM_EXECUTABLE` override `playwright.config.ts` documents, because the image's Chromium build predates the pinned Playwright). Accepted: the pending-scope notice is one plain string rather than `<code>`-wrapped ids, so the text matcher can read it.
- **T22** landed: five frozen dataclasses in `run_core.py` (`DraftUnitPaths`, `DraftUnitStatus`, `AssembledStatus`, `DraftProgress` with derived counts, `AssembleResult`), `_RunMode.supports_draft_units`, and the new `runs_draft_units.py` mixin (1071 lines: `draft_unit_paths` is the only speller of `<run>/draft/`; `write_skeleton_draft_prompt`, `write_module_draft_prompts`, `ingest_draft_unit`, `edit_draft_unit`, `assemble_draft`, `draft_progress`; module order from the approved outline's contract block; staleness hashes the contract-file entry and skeleton stub, never the live outline; a prompt batch appends its N events in one manifest critical section). `runs.py` net +34: the unit arms sit inside the draft slot of the unbound-stages loop gated on "not approved and no response file", `write_draft_prompt` also writes the skeleton unit, and `advance` delegates draft's `write_prompt` to the unit writers. Follow-up in the same thread: the all-modules-saved state returns a new action `assemble`, which `advance` performs (deterministic, like validate/finalize) and which raises `ConfigError` on an assembly error; recorded as decision 8b in the spec. Red: 59 tests (Sonnet); green: Opus, three mutations all caught, one red fixture fixed (a legacy case never saved its topic). No Phase 1 characterization pin changed; the 20-topic poll budget is untouched because the unit arms are unreachable once draft is approved. Suite 1901 passed / 1 skipped. Accepted: response events spell `response_sha256` explicitly rather than through the `files` mechanism; `DraftProgress.total` counts orphaned units while the "k of N" detail counts contract modules; the 7b rebuild leaves orphaned unit responses on disk (ignored by assembly, never deleted); the assembly-failure state stays `save_response` because fixing a module is a human step.
- **T23** landed: `Job.unit/module_id/batch_id` (legacy records load as `None`), `JobStore.active_unit_for` and `batch` beside an unchanged `active_for`, `JobStore.create` keyword-only unit fields, `JobRunner.execute` dispatching on `job.unit` to `draft_unit_paths`/`ingest_draft_unit` with `draft.<module>.failed.<ts>.txt` salvage, `ModelPlan.parallelism` (top-level `model-plan.toml` key, 1..4, default 2, bool rejected, emit omits the default, overrides carry it through), and the `Worker` pool: `parallelism` threads over the one FIFO, the whole admission rule in `_admissible` (nothing running, or every running job shares the candidate's `batch_id`), evaluated after dequeue with a condition variable that shares the worker lock, never re-queued, notified on every completion and cancel; `cancel_batch`; `daemon.worker_parallelism(root)` read once at `serve()`. Red: 44 tests (Sonnet); green: Opus. Mutation pass: admission-in-`enqueue` and re-queue-to-tail both survived the red set and are now pinned (`enqueue` must not wait while the workspace lock is held; a parked thread must stop consuming so peak concurrency stays at the batch's share); the non-batch-overlap mutation was caught. Timing tests ran green three times in a row (plus five standalone). Suite 1947 passed / 1 skipped. Accepted: unit jobs still write stage provenance for `draft`, last module wins; `parallelism` takes effect on daemon restart; `worker_parallelism` falls back to 2 on an unloadable plan (the error still surfaces per job); the concurrency bound is the thread count, so it depends on `start()` creating exactly `parallelism` threads.
- **T24** landed: `enqueue_stage(..., modules=None)` fans draft out (skeleton job while the skeleton has no response; otherwise one module job per `prompt_written`/`stale` module, or the `modules` subset with `force` required to rerun a saved module, all under one `batch_id` in contract order inside the existing lock, guards unchanged); routes `GET/POST /v1/jobs/batch/{id}[/cancel]`, `POST|PUT /v1/runs/{id}/draft/skeleton/response` and `…/draft/modules/{id}/response`, `POST /v1/runs/{id}/draft/assemble`, `modules` on `POST /v1/jobs` (batch response = primary job dict plus `batch_id` and `jobs`, ordered by `metadata.batch_index` because same-second job ids do not sort); `write_api.ingest_draft_unit/edit_draft_unit/assemble_draft`; `draft_progress` in `run_status_payload` with per-unit `job_id`; `parallelism` in `plan_payload`/`update_global_plan`, rejected by `update_run_plan`; `DaemonClient` methods; CLI `run --modules`/batch lines/`--wait` over the batch, `status` skeleton and `draft: k of N modules` lines, `cancel --batch`. Docs: `docs/providers.md` "Drafting a course module by module" and the daemon design's route table. Red: 61 tests (Sonnet); green: Opus, two of three mutations caught, the assemble route's missing active-job guard pinned. Three red tests fixed as unpassable-as-written (a helper that re-stamped the topic id, and two live-worker timing races given the bounded polls their siblings already use). Suite 2009 passed / 1 skipped; server/e2e/cli files green twice. Accepted: `draft_progress` on every status poll adds ~0.2 ms per topic after trimming it (no outline parse before a skeleton response, no response hashing before an assembly), so the local 20-topic budget in `tests/test_final_validation_cache.py` widened 50 → 70 ms (baseline measured 45.8 ms; CI keeps its 200 ms budget; the zero-revalidation call-count pins are untouched) — recorded in the ledger as the one performance cost of the phase.
- **T25** landed: `DraftProgressPanel` (skeleton row with its own paste control, one row per module with state, error, Rerun and paste; "Run modules with provider" as a batch; "Assemble draft"; superseded note; "k of N modules saved"; batch progress and "Cancel batch"), mounted from the run board when `draft_progress` is present; `PrimaryAction` renders the new `assemble` action and, once module rows exist, relabels to "Run modules with provider" and hands the paste loop to the panel (skeleton phase and legacy runs unchanged); `continueRun` treats `assemble` as an advance step and reports "n module jobs started" for a batch; Settings gains the `parallelism` input; the stage viewer shows a "Module contents" anchor nav for a drafted guide; client methods for the unit routes. Red: 23 vitest cases (Sonnet); green: Sonnet, one red-test fix (`userEvent.type` needs `{{` for literal braces). Gates on the merged branch: pytest 2012 passed / 1 skipped, `npm run build` clean, vitest 596, Playwright 88 passed including the new "guide run drafts module by module through the paste loop". Accepted: the stage-level paste loop still writes the whole-stage response (the deliberate whole-guide bypass the older e2e relies on), so the skeleton gets its own paste control in the panel; the per-module "changes since last run" diff is deferred (`response.previous.json` is written by the engine but not yet read by the viewer); draft content stays plain JSON text, so anchors live on the nav only.
- **T28** landed: the shipped example drafts through the unit path — `responses/draft.guide.json` split into `draft.skeleton.json` and `draft.modules/<id>.json`, `build_example.py` drives skeleton → module prompts → module ingests → assembly, `repair.guide.json` canonicalized to the assembled bytes so "repair resubmits the draft unchanged" stays byte-true, and three new pins in `test_example_project.py` (unit assembly determinism, unit layout, regeneration through the fan-out); the export is byte-identical because finalize re-canonicalizes its input. Docs: `docs/interactive-guides.md` (T22), `docs/providers.md` (T24), the example README, `docs/install-and-first-course.md` and `CLAUDE.md`. Suite 2012 passed / 1 skipped.

## Phase closeout

All nine threads merged into `claude/p2-engineering-manager-90ex44`, one merge commit per thread (T21/T26 and T22/T27 and T24/T25 ran in parallel worktrees, merged in plan order). Final gate on the merged branch: pytest 2012 passed / 1 skipped on Python 3.11 (baseline 1775; +237 across canonical/parse/prompt, draft-unit, characterization, job, server, client, CLI and example tests), `python3 -m education_pipeline --help` clean, `npm run build` clean, vitest 596 (baseline 554), Playwright 88 (baseline 87). Diff against `main` at `55cfc82`: 68 files, +11790 / −584.

Shape after the phase: draft on an interactive-guide run is a skeleton unit plus one unit per outline module under `<run>/draft/`, assembled by `assemble_guide` into the unchanged `responses/draft.response.json`; the stage graph, the single draft approval, draft validation and every downstream source binding are untouched, and a whole guide pasted into the response file still wins. The daemon runs module jobs as a batch under a `parallelism`-bounded pool that never overlaps anything else. Repair scopes are `RepairScope(module_id, section_id)` end to end (engine, API, CLI, cockpit). `runs.py` 2179 → 2282 lines (+103 for the unit arms and the `assemble` action); the unit lifecycle lives in `runs_draft_units.py` (1073).

Departures from the opportunity map, all recorded in the ledger: a skeleton call precedes the module fan-out (decision 2); the whole-guide prompt is still written (decision 5); per-module staleness keys on the contract file, with a separate rebuild arm for outline re-approval before draft approval (decision 7/7b); `advance` performs assembly through a new `assemble` action (decision 8b).
